const BASE_AUTH_ROUTE = "/auth";
const BASE_USER_ROUTE = "/user";

export const AUTH_ROUTES = {
  LOGIN: BASE_AUTH_ROUTE + "/login",
  REGISTER: BASE_AUTH_ROUTE + "/register",
};

export const USER_ROUTES = {
  PROFILE: BASE_USER_ROUTE + "/profile/:id",
  PROFILE: BASE_USER_ROUTE + "/profile/:id",
};
